###This project is used to config vim automatically using mixture bash and vim script

**running in debian-based, because using `apt-get` **

###Begin configuration running following command:
	bash vimconfig.sh
